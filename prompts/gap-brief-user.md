Given this knowledge gap and context, generate a research brief.

Primary gap: "{{primaryGap}}"
Related gaps: {{relatedGaps}}
Known facts: {{knownFacts}}

Respond with JSON:
{
  "background": "2-3 sentences summarizing known facts",
  "gap": "formulate what is missing based on the gaps",
  "suggestions": ["3-5 concrete research actions"]
}