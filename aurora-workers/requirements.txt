trafilatura>=1.8.0
pypdfium2>=4.0.0
soundfile>=0.12.0

# MCP server
mcp>=1.0.0

# WhisperX (replaces faster-whisper for GPU + word alignment + diarization)
whisperx @ git+https://github.com/m-bain/whisperX.git
torch>=2.0.0
torchaudio>=2.0.0
